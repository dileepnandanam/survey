<%= form_for(@textanswer) do |f| %>
  <% if @textanswer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@textanswer.errors.count, "error") %> prohibited this textanswer from being saved:</h2>

      <ul>
      <% @textanswer.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </div>
  <div class="field">
    <%= f.label :question_id %><br />
    <%= f.number_field :question_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
